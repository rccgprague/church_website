import { DefaultTheme } from "styled-components";
import Colors from "./color";

const Theme: DefaultTheme = {
  colors: {
    dark: Colors.dark,
    black: Colors.black,
    darkGrey: Colors.solidGrey,
    grey: Colors.grey,
    white: Colors.white,
    smoke: Colors.smoke,
    orange: Colors.orange,
    smokeOrange: Colors.smokeOrange,
    blue: Colors.blue,
    light: Colors.light,
    secondaryWhite: Colors.secondaryWhite,
    gray: Colors.gray,
    graybg: Colors.graybg,
  },
};

export default Theme;
