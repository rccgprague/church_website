export type LiveResponse = {
  youtubeId: string;
  startDateTime: Date;
  isLive: boolean;
};

export type YTVideo = {
  videoId: string;
  title: string;
  thumbnail: string;
  publishedAt: string;
};
